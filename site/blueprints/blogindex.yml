title: Blog Index

pages: 
  template: blogpost
  sort: date desc
  num: date

files: true

fields:
  headerHeadline:
    label: Header Settings 
    type: headline

  title:
    label: Title
    type: text
    required: true

  subtitle:
    label: Subtitle
    type: text
    help: Customize the site's subtitle. Leave blank to use page title.

  headerImage:
    label: Header Image
    type: image
    help: Select an image to customize the background of the header section (and within sub-pages).